<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.murad.project1">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_icon_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_icon_app"
        android:supportsRtl="false"
        android:theme="@style/AppTheme"
        tools:replace="android:supportsRtl">
        <activity android:name=".activites.student_activites.ui.Tracking"></activity>
        <activity android:name=".activites.restPassword" />
        <activity android:name=".activites.Splash" />
        <activity
            android:name=".activites.student_activites.studentGate"
            android:label="@string/title_activity_student_gate"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activites.studentApplicationActivity" />
        <activity android:name=".activites.StudentDetails" />
        <activity
            android:name=".activites.Index"
            android:label="@string/title_activity_index" />
        <activity android:name=".activites.PermissionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activites.catalog_signUp" />
        <activity android:name=".activites.register" />
        <activity android:name=".activites.LoginActivity" />
        <service android:name=".supportClasses.LocationService"
                  android:enabled="true"
            android:exported="true"
            android:stopWithTask="false"
            />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDkZqbA2FsOuDnide4rjdSN9kSTKHFnq3Y" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>